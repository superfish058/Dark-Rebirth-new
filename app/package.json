{
  "name": "dark-rebirth-app",
  "private": true,
  "version": "1.0.0",
  "description": "Dark Rebirth 桌面端与安卓端客户端（Capacitor 壳，加载前端应用）",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "cap:sync": "cap sync",
    "cap:open:windows": "cap open @capacitor/windows",
    "cap:open:android": "cap open @capacitor/android",
    "cap:build:windows": "vite build && cap sync @capacitor/windows && cap open @capacitor/windows",
    "cap:build:android": "vite build && cap sync @capacitor/android && cap open @capacitor/android"
  },
  "dependencies": {
    "@capacitor/app": "^7.0.1",
    "@capacitor/core": "^7.2.0",
    "@capacitor/haptics": "^7.0.1",
    "@capacitor/status-bar": "^7.0.1",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@capacitor-community/electron": "^5.0.1",
    "@capacitor/cli": "^7.2.0",
    "@vitejs/plugin-vue": "^5.2.4",
    "vite": "^6.3.5"
  }
}
